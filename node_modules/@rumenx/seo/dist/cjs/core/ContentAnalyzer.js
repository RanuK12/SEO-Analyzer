"use strict";
/**
 * Content analyzer for extracting and analyzing content structure and metrics
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ContentAnalyzer = void 0;
const HtmlParser_1 = require("../utils/HtmlParser");
/**
 * Content analyzer for SEO optimization
 */
class ContentAnalyzer {
    constructor(baseUrl) {
        Object.defineProperty(this, "baseUrl", {
            enumerable: true,
            configurable: true,
            writable: true,
            value: void 0
        });
        this.baseUrl = baseUrl;
    }
    /**
     * Analyze HTML content and extract comprehensive metrics
     */
    analyze(content, metadata = {}) {
        const startTime = Date.now();
        // Extract basic content
        const textContent = (0, HtmlParser_1.extractTextContent)(content);
        const wordCount = textContent.split(/\s+/).filter(Boolean).length;
        const characterCount = textContent.length;
        const sentenceCount = (0, HtmlParser_1.countSentences)(textContent);
        const paragraphCount = (0, HtmlParser_1.countParagraphs)(content);
        const readingTime = (0, HtmlParser_1.calculateReadingTime)(textContent);
        // Extract structure
        const headings = (0, HtmlParser_1.extractHeadings)(content);
        const images = (0, HtmlParser_1.extractImages)(content, this.baseUrl);
        const links = (0, HtmlParser_1.extractLinks)(content, this.baseUrl);
        // Extract SEO metrics
        const seoMetrics = (0, HtmlParser_1.extractSeoMetrics)(content);
        // Extract keywords
        const keywords = (0, HtmlParser_1.extractKeywords)(textContent);
        const keywordDensity = (0, HtmlParser_1.calculateKeywordDensity)(textContent, keywords);
        const frequentWords = (0, HtmlParser_1.getFrequentWords)(textContent);
        // Detect language (simple heuristic)
        const language = this.detectLanguage(textContent) ?? metadata.language ?? 'en';
        // Build content structure
        const structure = {
            headings,
            images,
            links,
            sections: this.extractSections(content),
        };
        const processingTime = Date.now() - startTime;
        return {
            rawContent: content,
            textContent,
            language,
            wordCount,
            characterCount,
            sentenceCount,
            paragraphCount,
            readingTime,
            keywords,
            keywordDensity,
            frequentWords,
            structure,
            seoMetrics,
            meta: {
                analyzedAt: new Date(),
                processingTime,
                version: '1.0.0',
                mode: 'full',
            },
        };
    }
    /**
     * Fast analysis with minimal processing
     */
    analyzeFast(content, metadata = {}) {
        const startTime = Date.now();
        // Extract basic content only
        const textContent = (0, HtmlParser_1.extractTextContent)(content);
        const wordCount = textContent.split(/\s+/).filter(Boolean).length;
        const characterCount = textContent.length;
        const readingTime = (0, HtmlParser_1.calculateReadingTime)(textContent);
        // Extract minimal structure
        const headings = (0, HtmlParser_1.extractHeadings)(content);
        const seoMetrics = (0, HtmlParser_1.extractSeoMetrics)(content);
        // Basic keywords extraction
        const keywords = (0, HtmlParser_1.extractKeywords)(textContent, 3, 10);
        const processingTime = Date.now() - startTime;
        return {
            rawContent: content,
            textContent,
            language: metadata.language ?? 'en',
            wordCount,
            characterCount,
            sentenceCount: 0,
            paragraphCount: 0,
            readingTime,
            keywords,
            keywordDensity: {},
            frequentWords: [],
            structure: {
                headings,
                images: [],
                links: [],
                sections: [],
            },
            seoMetrics,
            meta: {
                analyzedAt: new Date(),
                processingTime,
                version: '1.0.0',
                mode: 'fast',
            },
        };
    }
    /**
     * Extract content sections
     */
    extractSections(content) {
        const sections = [];
        // Simple section extraction based on common HTML patterns
        const patterns = [
            { type: 'header', selector: 'header' },
            { type: 'nav', selector: 'nav' },
            { type: 'main', selector: 'main' },
            { type: 'article', selector: 'article' },
            { type: 'section', selector: 'section' },
            { type: 'aside', selector: 'aside' },
            { type: 'footer', selector: 'footer' },
        ];
        patterns.forEach(pattern => {
            const regex = new RegExp(`<${pattern.selector}[^>]*>([\\s\\S]*?)<\\/${pattern.selector}>`, 'gi');
            let match;
            while ((match = regex.exec(content)) !== null) {
                const matchContent = match[1];
                if (!matchContent)
                    continue;
                const sectionContent = (0, HtmlParser_1.extractTextContent)(matchContent);
                const wordCount = sectionContent.split(/\s+/).filter(Boolean).length;
                if (wordCount > 0) {
                    sections.push({
                        type: pattern.type,
                        content: sectionContent,
                        wordCount,
                    });
                }
            }
        });
        return sections;
    }
    /**
     * Simple language detection based on common words
     */
    detectLanguage(text) {
        const sample = text.toLowerCase().substring(0, 1000);
        // Common words for different languages
        const languagePatterns = {
            en: ['the', 'and', 'or', 'but', 'in', 'on', 'at', 'to', 'for', 'of', 'with', 'by'],
            es: ['el', 'la', 'de', 'que', 'y', 'en', 'un', 'es', 'se', 'no', 'te', 'lo'],
            fr: ['le', 'de', 'et', 'à', 'un', 'il', 'être', 'et', 'en', 'avoir', 'que', 'pour'],
            de: ['der', 'die', 'und', 'in', 'den', 'von', 'zu', 'das', 'mit', 'sich', 'des', 'auf'],
            it: ['il', 'di', 'che', 'e', 'la', 'per', 'in', 'un', 'è', 'non', 'con', 'da'],
        };
        let maxScore = 0;
        let detectedLanguage;
        Object.entries(languagePatterns).forEach(([lang, words]) => {
            let score = 0;
            words.forEach(word => {
                const regex = new RegExp(`\\b${word}\\b`, 'g');
                const matches = sample.match(regex);
                score += matches ? matches.length : 0;
            });
            if (score > maxScore) {
                maxScore = score;
                detectedLanguage = lang;
            }
        });
        // Return detected language only if we have reasonable confidence
        return maxScore > 5 ? detectedLanguage : undefined;
    }
}
exports.ContentAnalyzer = ContentAnalyzer;
//# sourceMappingURL=ContentAnalyzer.js.map